/** Host shim for the browser-only image theme plugin. */
export const name = 'image-theme'

/** No host services are required; all behavior runs inside the web client. */
export function apply(): void {}
